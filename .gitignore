# Binaries
warpbox.exe
/warpbox
*.exe
*.test
*.out

# Go workspace
go.work
go.work.sum

# Config
/config.yml

# Runtime data directory (config.yml + warpbox.db)
data/

# SQLite databases
*.db
*.db-*

# Vendored dependencies (generated by go mod vendor for dev builds)
vendor/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# CI / test artefacts
*.log

# Audit reports (generated by audit script)
reports/

# OS
.DS_Store
Thumbs.db

# Standalone Boxarr ZimaOS stack (published as git branch boxarr-zimaos)
/boxarr-zimaos/

# Sensitive credentials (keep local only — use .template for structure)
docs/auth-matrix.md

# Debug artifacts (board ops testing)
board_analysis.txt
board_edit.html
board_page.html
issue2.html
test_results.txt
$tmp

# Internal tooling (kept on internal branch only)
.gitea/
.clinerules/
audit script
board.html
dev-deploy script
docker-build.sh
gitea_web.go
index.js
integration test
intensive test
js_search.txt
library_test.ps1
name
torbox_before.txt
torbox_new.txt
usenet_sample.json
internal/server/warpbox-sm.png
.git-rewrite/
